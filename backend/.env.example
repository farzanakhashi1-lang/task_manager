## Example environment variables for connecting to MongoDB Atlas
# Copy this file to `.env` and fill in values before running the server.

# MongoDB connection string (SRV URI)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.pd8tjwi.mongodb.net/?appName=Cluster0

# Database name to use in Atlas
MONGODB_DB_NAME=task_manager
